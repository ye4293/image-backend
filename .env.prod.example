# 生产部署环境变量。**服务器上复制成 `.env`** 后逐项填写，别提交填好的那份。
#
#   scp .env.prod 用户@服务器:/image-backend/.env
#
# 为什么是 .env 而不是 .env.prod：compose 不带 --env-file 时自动读 .env，于是部署
# 命令就是干净的 `docker compose up -d`。用别的名字每条命令都得跟 --env-file，
# 而敲漏一次会用空值启动——后端退化成临时 SQLite，服务照常跑但重启丢全部数据。
#
# 有两条部署路线，用哪条决定了下面「数据库」一节填哪几项：
#
#   A. 外部托管数据库（默认，当前 moloom 生产用的就是这条）
#      docker compose up -d
#      → 填 DATABASE_URL 整串，POSTGRES_* 三项**不被读取**
#
#   B. compose 自带 Postgres 容器
#      docker compose -f docker-compose.with-postgres.yml up -d
#      → 填 POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB，DATABASE_URL 由 compose 拼
#
# **新增环境变量时，两个 compose 文件都要同步改**（除了仅属于某一条路线的变量，
# 如 POSTGRES_* 只在 with-postgres 那份里）—— 只填进 .env 是不够的：env 文件里的
# 变量只用于 compose 自己做 ${} 插值，不会自动进容器，漏传的后果是静默的。
#
# 三个 compose 文件：
#   docker-compose.yml                生产默认，外部托管数据库（本文件路线 A）
#   docker-compose.with-postgres.yml  生产备选，自带 Postgres 容器（路线 B）
#   docker-compose.dev.yml            本地开发，只起 Postgres、不含后端

# ---- 数据库 ----
# 路线 B（compose 自带 Postgres 容器）填这三项。走默认的路线 A 时这三项**不被读取**，
# 留空即可——刻意保留而不删掉，是因为删了会让人以为漏配了，回头又去补一遍。
POSTGRES_USER=imageapp
# 换成随机强密码。库不对外暴露端口，但弱密码在容器逃逸或内网横向时仍是短板。
# 生成：openssl rand -base64 32
POSTGRES_PASSWORD=
POSTGRES_DB=imageapp

# 路线 A（外部托管数据库，默认）填这一整串，上面三项留空即可。
#
# ⚠️ **走路线 A 时这一项漏填的后果是静默丢数据。** docker-compose.yml 用
#    ${DATABASE_URL} 且无默认值，compose 只会打一行 warning 就代入空串；后端拿到
#    空值会退化成**进程内临时 SQLite**，服务照常起来、注册和生成都能用，但重启即丢
#    全部用户、次数、订阅与已播种的密钥。同时 main.go 的弱 JWT 密钥检查条件是
#    DatabaseURL != ""，空值会让那道防线一起失效。
#
# 格式：postgres://用户名:密码@主机:5432/库名?sslmode=require
#   · 必须以 postgres:// 或 postgresql:// 开头，否则会被当成 SQLite 文件路径，
#     而那个失败长得像 "unable to open database file"，完全指不到真正的原因。
#   · 密码含 @ : / ? # 时必须 URL 编码（@ → %40）；忘了转义的表现是"认证失败"
#     或"库不存在"，而不是"URL 格式错误"。
#   · sslmode：托管实例默认关 TLS 的情况很常见（阿里云 PolarDB 出厂就是 ssl=off），
#     此时填 require 会被服务端直接拒绝、启动即失败。走公网就先在控制台开 SSL。
DATABASE_URL=

# ---- 认证 ----
# 必须换掉。后端在连接真实数据库时若发现是默认值 dev-secret-change-me 会拒绝启动。
# 生成：openssl rand -base64 48
JWT_SECRET=

# ---- 配置加密主密钥 ----
# 加密 app_settings 表里 secret 项（上游 key、R2 密钥）的主密钥。
# **必填，缺了或长度不对会拒绝启动**——解不开的密文等于所有上游凭据失效，
# 那时起来只会以「上游认证失败」的形式误导排查。
# 生成：openssl rand -base64 32
#
# 它和数据库备份要**一起**备份。丢了它，库里的 secret 无法恢复，只能登进后台重填。
CONFIG_ENCRYPTION_KEY=

# ---- 上游 ----
# ⚠️ 从本版起，下面的 FLUX_API_KEY / EZLINKAI_BASE_URL / R2_* / APP_BASE_URL
#    **只在首次启动、且 app_settings 表里还没有该项时**被读取一次（播种）。
#    之后一律以数据库为准，请在后台设置页修改；改这里不再生效。
#    这样设计是为了让「当前生效的是哪个值」永远只有一个答案。
EZLINKAI_BASE_URL=https://api.ezlinkai.com
# 留空会退化成 stub adapter（出占位图，不调上游、不花钱）。
# 生产必须填；填错或过期时后端日志会明确打出「认证失败」而不是笼统的上游错误
# ——那个区分是刻意的：key 死了会让 100% 请求失败，表现却和「prompt 被安全过滤
# 拒绝」一模一样，混在一起就没有任何信号能区分两者。
FLUX_API_KEY=

# ---- 引导第一个管理员 ----
# 用该邮箱在站上注册后即成为管理员（后台发次数、改模型扣费都要管理员）。
# 库里一旦已有管理员，这个变量就自动失效，所以用完不删也安全（见 README）。
BOOTSTRAP_ADMIN_EMAIL=

# ---- Stripe ----
# Dashboard → 开发者 → API 密钥 → 密钥。生产用 sk_live_ 开头。
# live 密钥配合 localhost 的 APP_BASE_URL 会拒绝启动，所以 APP_BASE_URL 必须是
# 真实前端域名。
STRIPE_SECRET_KEY=
# Dashboard → 开发者 → Webhooks → 对应 endpoint 的 Signing secret（whsec_ 开头）。
# endpoint 地址填 https://<你的后端域名>/api/v1/webhooks/stripe
# 与本地 `stripe listen` 打印的值**不是同一个**，混用会导致验签一直失败。
STRIPE_WEBHOOK_SECRET=
# 前端地址，用于拼 Checkout 成功/取消的跳转。换成你的真实前端域名。
APP_BASE_URL=https://app.example.com

# ---- CORS ----
# 允许跨域直连本后端的前端来源，逗号分隔。**只支持精确匹配，不支持通配符。**
#
# **前端与后端不同源时必填**（前端在 Vercel、后端在自己服务器就是这种）。
# 后端认证走 Authorization 头，那是"非简单请求头"，于是浏览器在每个 API 请求前
# 都会先发一次 OPTIONS 预检；没有这一项，预检拿不到 CORS 头，浏览器会拦掉全部请求。
#
# 漏填的表现极具误导性：进程正常启动、**curl 测后端全部正常**，只有浏览器里全挂，
# 而反代日志里只有一串被拒的 OPTIONS（403），没有任何信号指向缺 CORS。启动日志里
# 会有一行 "cors: CORS_ALLOWED_ORIGINS 未配置" 的告警——上线后请确认它**没有**出现。
#
# 反过来，下面几种写法会**拒绝启动**（都属于"不报错、只静默失效"那一类）：
#   · *                    —— 它能正常工作，只是把 API 对全网敞开
#   · https://*.vercel.app —— 任何含 * 的写法都拒绝，理由见下
#   · moloom.ai            —— 缺 scheme，永远匹配不上
#   · https://x.com/       —— 尾斜杠，Origin 头永远不带，永远匹配不上
#   · https://u@x.com      —— userinfo；https://x.com:abc —— 非法端口，同上
#
# 为什么不支持通配符（一次代码审查后的刻意回退，别再加回来）：后缀匹配无法可靠地
# 锚定在 DNS 标签边界上。https://*moloom.ai（比 https://*.moloom.ai 少一个点）会放行
# 攻击者注册的 evilmoloom.ai；而"后缀必须含点号"会被通配符自带的分隔点满足，
# 于是 https://*.com 一路通过，等于放行整个 TLD。
#
# 注意子域**不自动继承**：配了 https://example.com 不会放行 https://app.example.com。
# 端口与 scheme 不同即不同 origin，也要分别列出。
#
# Vercel preview 若需在浏览器里直连后端，正解是用 Vercel 的 Preview Deployment
# Suffix 把预览放到自有域名下，再逐个列进来。
#
# 同源部署（nginx 同时服务前端与 /api）时留空才是对的。
CORS_ALLOWED_ORIGINS=

# ---- 图片转存（Cloudflare R2）----
# 前置人工步骤：开通 R2 → 建桶 → 给桶绑自定义域 → **开启公开访问**。
#
# 为什么上游网关已经转存过、这里仍然建议配上：网关的 get_result 只在它自己的
# webhook 处理完转存之后才返回永久 URL，否则返回上游原始 sample——那是 10 分钟
# 后失效的临时链接。我们的轮询间隔是 3 秒，撞上那个窗口的概率不低。配上这五项
# 等于把拿到手的任何链接都复制成永久的，竞态被消掉；代价是每张图多存一份。
#
# 五项必须齐全，缺任何一项都退化成不转存——那时 image_url 存的是上游临时链接、
# 约一小时后失效，历史页于是全是死链，而用户为那些图付过费。
#
# Cloudflare → R2 → 概览页的「S3 API」地址
R2_ENDPOINT=
# R2 → 管理 API 令牌 → 创建（权限选「对象读和写」）
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
# 绑在桶上的自定义域，**必须带 https://**。
# **不能填 R2_ENDPOINT 的值**：S3 endpoint 不允许匿名读，拿它拼出来的图片 URL
# 每一个都会 401，而上传全部成功、库里 stored=true，等发现时已经攒了一批 URL
# 全错的记录（对象是好的，得写脚本回头改）。
# 下面三种填法都会在启动时被拦下、拒绝启动，不会静默产出坏数据：
#   · 其余四项填了、这项留空
#   · 填成 *.r2.cloudflarestorage.com
#   · 少了 https:// 前缀（拼出来的地址会被浏览器当成相对路径）
# *.r2.dev 是 R2 正经的公开域名，允许填。
R2_PUBLIC_BASE_URL=
