# 修改内容总结 · 2026-08-02 · miss

本轮做完了三件事：

1. **图片转存到 Cloudflare R2** —— 让图片 URL 不再一小时后失效
2. **生成历史接口与页面** —— 让用户能拿回自己付费生成过的图
3. **运营配置搬进后台设置页** —— 上游 key、R2 五项、前端跳转地址改完立即生效，不用改 `.env`、不用重启

两个仓库都已合并到 `main` 并推送（第 3 项的前端页面见文末状态）：

| 仓库 | 推送 | 说明 |
|---|---|---|
| `image-backend` | `0e29613 → 4308d53` | 转存+历史 22 个提交，设置页后端 8 个提交 |
| `image-front` | `2d50d90 → 582ac0c` | 转存+历史 5 个提交；设置页前端见文末 |

---

## 〇、部署前最重要的一条（本轮新增）

**`.env.prod` 必须新增 `CONFIG_ENCRYPTION_KEY`，否则服务拒绝启动。**

```bash
CONFIG_ENCRYPTION_KEY=$(openssl rand -base64 32)
```

它是加密数据库里那些 secret（上游 key、R2 密钥）的主密钥。缺了或长度不对会直接 `Fatal` ——这是刻意的：解不开的密文等于所有上游凭据失效，那时候起来只会以「上游认证失败」的形式把排查方向带偏。

**这把密钥要和数据库备份一起备份。** 丢了它，库里的 secret 无法恢复，只能登进后台重新填一遍。

### 还有一条行为变化必须知道

`FLUX_API_KEY` / `EZLINKAI_BASE_URL` / `R2_*` / `APP_BASE_URL` 这些 env 变量，**只在首次启动、且数据库里还没有该项时被读取一次**（播种），之后一律以数据库为准。

推论：**改 `.env` 再重启不会生效，也不会撤销之前播种的值。** 我在测试时就踩了这个——去掉 `FLUX_API_KEY` 重启后，库里那个值依然在，服务照旧用它。要改就去后台设置页改，或者直接改库。

这个设计是刻意的：如果做成「库里没有就回落到 env」，那「当前生效的到底是哪个值」会长期含糊，而那种含糊在排查配置问题时代价很高。

Stripe 的两个 secret 与 `JWT_SECRET` / `DATABASE_URL` **不在**后台，仍然只看 env（理由见第四节）。

---

## 一、部署前必须先做的事

### 1. compose 漏传 8 个环境变量（本轮已修，但要知道它意味着什么）

`docker-compose.prod.yml` 的 backend `environment:` 块原先**没有透传** `STRIPE_SECRET_KEY`、`STRIPE_WEBHOOK_SECRET`、`APP_BASE_URL`，以及五个 `R2_*`。

`--env-file` 里的变量只用于 compose 自己做 `${}` 插值，**不会自动进容器**。所以哪怕 `.env.prod` 填得完全正确，容器里也读不到这三个 Stripe 变量。后果是静默的：

- `billing.New("")` 返回 nil → `/billing/subscribe` 一律回 `503 billing is not configured` → **没人能订阅，一分钱收不到**，而日志只有一行「未配置」，看起来像是有意关掉的；
- webhook 同样 503，Stripe 会**无限重投**同一个事件。

这是 M4a 那轮就存在的问题，不是本轮引入的，但本轮修掉了。**如果你之前部署过并以为 Stripe 在工作，请重新确认。**

### 2. `.env.prod` 需要新增五项

```bash
R2_ENDPOINT=https://<account_id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_PUBLIC_BASE_URL=https://img.你的域名        # 绑在桶上的自定义域
```

**`R2_PUBLIC_BASE_URL` 不能填 `R2_ENDPOINT` 的值。** S3 endpoint 不允许匿名读，拿它拼出来的图片 URL 每一个都会 401。启动时有拦截：

- 有 R2 凭证但 `R2_PUBLIC_BASE_URL` 为空 → **拒绝启动**
- `R2_PUBLIC_BASE_URL` 填成 `*.r2.cloudflarestorage.com` → **拒绝启动**
- `R2_PUBLIC_BASE_URL` 少了 `https://` → **拒绝启动**（否则每个 URL 会变成相对路径）

`*.r2.dev` 是 R2 正经的公开域名，允许填。

前置人工步骤：Cloudflare 开通 R2 → 建桶 → 绑自定义域 → **开启公开访问**。

### 3. 建议清空 `generations` 表

本轮修了一个时区 bug（见第三节）。修复只作用于**新写入**的行；库里已有的行时间戳仍是本地时区字符串，翻页翻到它们时游标比较仍会出错。

设计文档记录存量是测试数据，所以直接清表最省事：

```sql
DELETE FROM generations;
```

如果里面有你要留的数据，告诉我，需要另写一次性迁移把 `created_at` 归一成 UTC。

### 4. 数据库迁移是自动的

`AutoMigrate` 会自己加 `generations.stored` 列、建 `(user_id, created_at)` 复合索引、并删掉并入后遗留的旧单列索引 `idx_generations_user_id`。**不需要手写迁移脚本。**

---

## 二、后端改了什么

### 新增接口

```
GET /api/v1/generations?cursor=<不透明串>&limit=<1..50，默认 20>
→ 200 {"generations":[...], "nextCursor": string | null}
→ 400 {"code":40000,"message":"invalid cursor"}
→ 401/403 同其他鉴权接口
```

- 游标分页，倒序。`limit` 越界**钳制不报错**。
- 只返回当前用户的记录。
- **不返回** `processing` 状态的行（避免用户看到永远转圈的格子）。
- **返回失败记录**，带 `error` 与 `creditsSpent: 0`——用户看到「我明明生成过一张」却找不到会怀疑被吞了钱，而失败记录恰恰证明没扣钱。

`POST /api/v1/generations` 的成功响应新增 `stored: boolean` 字段。

### 转存怎么工作

包在每个 provider adapter 外面的一层装饰器：上游出图后，在**同一个请求内**下载图片 → 嗅探内容类型 → 上传 R2 → 把永久 URL 写回。请求时长 +1~3 秒（Flux 本来约 21 秒）。

三个关键决定：

1. **转存失败一律降级，不退款、不判失败。** 图已经出了、钱已经花在上游了；因为我们自己的存储抖动就退款，等于把一次成功且已付费的调用白扔，用户还得重排 21 秒。降级最坏后果只是这张图一小时后失效。**这条违反直觉，别"修"掉它。**
2. **内容类型来自嗅探字节，绝不信上游的 `Content-Type` 头。** 这个字节流要挂在我们自己的域名下，上游若返回 HTML（哪怕头写着 `image/png`），我们就在自己 origin 上托管了别人可控的 HTML——那是 XSS。白名单只有 png/jpeg/webp。
3. **下载上限 20 MiB。** 无上限读进内存是内存耗尽向量。

`stored=false` 有两种来源：R2 未配置，或转存失败降级。两种情况下 `image_url` 都是上游临时链接，前端据此提示「链接可能已失效」。

### 修了一个真 bug：SQLite 时区导致翻页永远为空

`glebarez/sqlite` 把 `time.Time` 存成**带时区的字符串**。生产上 `created_at` 由 GORM 用本地时间填（`+08:00`），存成 `"14:xx+08:00"`；而游标参数是 UTC，`"06:xx+00:00"`。SQLite 做**字符串**比较得出 `"14" > "06"`，于是 `created_at < cursor` 恒假——**第二页永远是空的**。

修法：GORM 配 `NowFunc` 强制 UTC。

这个 bug 是靠浏览器端到端测试发现的，22 个后端提交、8 个包全绿的测试套件全都漏掉了它——因为所有分页测试都自己传 `time.Now().UTC()`，从未走过「GORM 按什么时区写库」这条生产路径。已补一条回归测试：刻意不设 `CreatedAt` 让 GORM 自己填，然后翻页断言拿到全部行。

---

## 三、前端改了什么

- 新页面 `/history`：网格展示历史，卡片三态——永久图 / 图+「链接可能已失效」/ 灰格子+「未扣除次数」。
- 分页用「加载更多」按钮，不用无限滚动（窄屏下无限滚动会让页脚永远够不着）。
- 四语文案齐全（en/zh/ja/ko，各 119 个 key，已脚本校验完全一致），无硬编码文案。
- `proxy.ts` 的登录守卫加了 `history`；顶栏加了入口。
- 顺带修了一个**先于本轮存在**的问题：`e2e/generate.spec.ts` 里 `DEFAULT_MODEL_COST = 1` 早已与后端播种的 7 credits 脱节，导致 e2e 套件一直红着 2 条。

---

## 三点五、后台设置页（第 3 件事）

### 后端

新增 `app_settings` 表（key/value）与 `internal/settings` 包四层：白名单 `spec`、AES-256-GCM 加解密 `crypto`、读写与播种 `store`、原子快照与客户端构造 `runtime`。

两个接口挂在已有的 admin 路由组下：

```
GET   /api/v1/admin/settings   → 非 secret 项给 value；secret 项只给 configured + masked
PATCH /api/v1/admin/settings   → 部分更新；未知 key 400；校验失败 400 且不生效
```

三个关键决定：

1. **secret 加密存库，主密钥只能来自 env。** 明文写库意味着它们会出现在 `pg_dump` 备份、任何有库读权限的人手里、以及排查时随手 `SELECT *` 的终端历史里。这次交换是把「6 个 secret 在 env」换成「1 个主密钥 + 2 个 Stripe secret 在 env」——`.env` 消不掉，只是变短。
2. **API 永不回传 secret 明文**，只给掩码（`••••••••cd12`，保留末四位便于辨认）与「是否已配置」。一旦回传明文，设置页就成了一个 secret 泄露端点，任何 XSS 或被盗的管理员会话都能一次读走全部凭据。
3. **改完立即生效，不重启。** `Runtime` 用 `atomic.Pointer` 持有已构造好的客户端，写入成功后整体原子替换。选无锁原子快照而不是加锁读，是因为生成请求会持有数十秒，加锁读会让改配置被长请求挡住。

**启动期拦截降级成告警**：库里的值可能是上一个管理员改坏的，那时拒绝启动等于让一次误操作把服务打死；正确行为是带着告警起来、让管理员登进去改回来。写入期校验才是主防线（它在坏数据产生**之前**）。唯一仍然 `Fatal` 的是主密钥非法。

### 前端

新增 `/admin/settings`（这是本仓库第一个 admin 页面）。**页面自己校验 `role === "admin"`** —— `proxy.ts` 只检查 cookie 存在，普通用户能直接访问该 URL，渲染出掩码与「已配置」状态就是信息泄露。

**一个反直觉但必须如此的行为**：secret 输入框留空表示「不修改」，而不是「清空」。因为后端把空字符串理解为清空，而浏览器从来拿不到真实值（只有掩码），所以输入框天然是空的——一个老实提交整个表单的实现，会让运营每改一次桶名就把上游 key 与两个 R2 密钥全部清空，而界面看起来一切正常。清空需要独立的按钮 + 二次确认。

这一条有单测和 e2e 双重守卫。我实测过：把客户端改成「提交整个表单」的错误实现后，两条测试立刻变红。

### 这一页刻意与其他页面不一致的一点

其他页面都不显示后端错误原文（那些是英文运维文案）。**设置页显示**——它的使用者是管理员，而「那是 S3 API 域名、不允许匿名读」正是他需要看到的原文。

---

## 四、验证到什么程度

| 项目 | 状态 |
|---|---|
| 后端 `go test ./...` | 9 包全绿（含新增 `internal/settings`） |
| 后端 `go vet` / `go build` | 干净 |
| 前端单测 | 61 通过 |
| 前端 `tsc` / `lint` / `build` | 干净 |
| 端到端（真实 Go 后端 + 真浏览器） | 转存+历史 18/18；设置页 7 条 |
| 375×667 实机截图 | 历史页与设置页均已人眼确认 |

### 后台设置页的六条手工验证（已实测通过）

1. GET 响应里 grep 完整明文，计数 0；掩码是 8 个圆点 + 末四位
2. `PATCH {"stripeSecretKey":...}` → 400（白名单边界在 HTTP 层生效，不只在单测里）
3. R2 公开域名填成 S3 API 域名、或缺 `https://` → 400 且带原因
4. **同一进程内**（PID 未变）补齐五项 → `storageEnabled` False→True、桶名从播种值变成后台值，**未重启**
5. secret 传空串 → `configured:false`，`storageEnabled` 正确跟着变 False
6. 直接改库塞入非法值（绕过校验）→ 重启后 HTTP 200、未 Fatal、逐条打出告警

另外直接读表确认：`fluxApiKey` / `r2AccessKeyId` 在库里是密文，非 secret 项是明文。

### 还没验证的（重要）

**R2 至今没有凭证，所以「图不会再一小时后失效」这句话目前没有任何证据。** 所有测试覆盖的是本地 HTTP 桩和 `NoopStorage`。

部署后请手工跑这四条：

1. 生成一张图 → 确认对象真的进了 R2 桶
2. 用自定义域直接打开那个 URL → 确认**匿名**可访问
3. 一小时后再打开一次 → 确认仍然有效（这才是整轮的目的）
4. 故意把 `R2_BUCKET` 填错 → 确认**生成仍然成功**、`stored=false`、日志有告警、**次数没有被退**

第 4 条最容易被跳过，而它守的正是那条最反直觉的决定。

---

## 五、没做 / 已知遗留

- **加量包（M4b）不做**（按你的决定）。`orders` 表、一次性支付、`payment_intent.succeeded` 都没有。定价页上加量包区域显示「暂未开售」。
- `isPublic` 仍是只写字段，公开画廊不存在。
- 历史页没有删除、收藏、搜索、按模型筛选——都不服务于「拿回自己的图」这个目标。
- 参考图上传仍是假的：只读文件名做本地预览，文件从不进请求体；后端 `POST /generations` 也没有图生图字段，但 `GET /models` 却在对外声明 `supportsImageToImage`。
- 登录无限流，上线前建议补。
- 后端错误 `message` 是英文，会穿透到四种语言的界面（历史页刻意不显示后端原文，用本地化文案兜住了，其他页面依旧）。
- ja/ko 译文未经母语审校。
- 深翻页时游标是后置过滤而非索引区间查找，第 N 页要扫约 (N−1)×页大小 行。亚毫秒级，不比 offset 分页差，**不是缺陷，不需要"优化"**。

---

## 六、部署命令

`docker-compose.prod.yml` 已把当前所有变量透传（Caddy 已移除，反代交给宿主机 nginx）。**新增环境变量时它必须同时改** —— 只填进 `.env.prod` 是不够的：`--env-file` 里的变量只用于 compose 自己做 `${}` 插值，不会自动进容器。

用外部托管数据库（阿里云 RDS / PolarDB 等）时改用 `docker-compose.external-db.yml`：它不起 postgres 容器，`DATABASE_URL` 整串由 `.env.prod` 提供。

```bash
git pull
cp .env.prod.example .env.prod
vim .env.prod        # 逐项填，见下面清单
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d --build
docker compose -f docker-compose.prod.yml logs -f backend
```

**必填清单**：`POSTGRES_PASSWORD`（`openssl rand -base64 32`）、`JWT_SECRET`（`openssl rand -base64 48`）、**`CONFIG_ENCRYPTION_KEY`（`openssl rand -base64 32`，本轮新增，缺了起不来）**、`FLUX_API_KEY`、`BOOTSTRAP_ADMIN_EMAIL`、三个 Stripe/`APP_BASE_URL`、五个 `R2_*`、**`CORS_ALLOWED_ORIGINS`（前后端不同源时必填）**。

走外部数据库时把 `POSTGRES_PASSWORD` 换成 **`DATABASE_URL`** 整串 —— 漏填它不会报错，后端会退化成进程内临时 SQLite，重启即丢全部数据。

nginx 侧照 `deploy/nginx.conf.example` 配。**`proxy_read_timeout` 必须调到 300s**——nginx 默认 60 秒，而生成要挂 25~35 秒、慢模型最长 3 分钟；不调会在快出图时给用户 504，而后端用的是脱离请求的 context，图照样出完、次数照样已扣。

首次部署若库里已有旧数据，先 `DELETE FROM generations;`（时区修复只作用于新写入的行）。

### 启动日志要确认三件事

- 没有 `config: ...` 开头的 `Fatal`（配置拦截）
- 若看到 `settings: 从环境变量播种了 N 项配置` —— 正常，说明首次播种成功，**今后以数据库为准**
- **不该**看到 `settings: R2 未完整配置，图片不转存` —— 看到说明五项没齐，图片仍会一小时后失效
- 前后端不同源时，**不该**看到 `cors: CORS_ALLOWED_ORIGINS 未配置` —— 看到说明浏览器会拦掉全部请求，而 curl 测起来一切正常
- 走 `docker-compose.external-db.yml` 时，**不该**看到 `database: 临时 SQLite` —— 看到说明 `DATABASE_URL` 没进容器，服务能正常跑但重启即丢全部数据

### 起来之后的验证顺序

1. 生成一张图，然后 `SELECT id, stored, image_url FROM generations ORDER BY created_at DESC LIMIT 1;` —— 要 `stored = true` 且 `image_url` 是你自己的 `R2_PUBLIC_BASE_URL` 域名
2. `/history` 能看到那张图，且**没有**橙色「链接可能已失效」提示
3. 无痕窗口直接打开那个 `image_url`，确认**匿名**可访问
4. **一小时后再打开一次** —— 这才是整轮的目的
5. 用 `BOOTSTRAP_ADMIN_EMAIL` 那个账号登录，打开 `/admin/settings`，确认八个字段都在、secret 显示为掩码
6. 真实测试卡走一次订阅，确认 `/billing/subscribe` 不再回 503（那三个变量之前是漏传的）

第 4、6 条目前还没有任何东西证明过，只能你部署后做。
